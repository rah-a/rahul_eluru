#include<stdio.h>
int main()
{
	int num;
	printf("entre a integer:");
	scanf("%d",&num);
	if(num%3==0)
	{
	printf("the given number is divisiable by 3\n");	
	}
	else
	{
		printf("the number is not divisiable by 3\n and gives a reminder %d\n",num%3);
	}
return 0;
}
